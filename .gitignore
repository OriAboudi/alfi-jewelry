node_modules
dist
dist-ssr
.env
.env.local
*.local
.DS_Store
npm-debug.log*
yarn-error.log*
.vscode/*
!.vscode/extensions.json
supabase/.temp

# Local Netlify folder
.netlify

# Local AI-tool skill installs (Impeccable, etc.) — machine-local tooling,
# not website source; large binaries, not meant to be shared via git.
.claude
.agents
.codex
skills-lock.json
