{
  "name": "alfi-jewelry-store",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "ALFI — חנות תכשיטי כסף (React + Vite + Supabase)",
  "scripts": {
    "dev": "vite",
    "prebuild": "node scripts/generate-sitemap.mjs && node scripts/inject-hero-preload.mjs",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "@supabase/supabase-js": "^2.45.4"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.2"
  }
}
